# Used only when packing the Windows installer. Do not commit real production URLs
# if you want them off git — set the variable in the shell instead.
#
#   set VITE_SERVER_URL=https://spots.example.com
#   npm run pack
#
# VITE_SERVER_URL=https://spots.example.com
# VITE_ALLOWED_SERVERS=
